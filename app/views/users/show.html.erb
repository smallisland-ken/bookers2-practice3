<div class="container">
  <div class="row">
    <div class="col-md-3">
      <%= render 'users/user_template', user:@user %>
      <%= render 'books/book_template', book:@book %>
    </div>
    <div class="col-md8 offset-md-1">
      <table class="talbe">
        <thead>
          <tr>
            <th></th>
            <th>Title</th>
            <th>Opinion</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% @bookuser.each do |f| %>
          <tr>
            <td><%= link_to user_path(f.user) do %><%= attachment_image_tag @</td>
            <td></td>
            <td></td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>